<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Challenge 3 - SQL Injection</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/header') %>

    <div class="container">
        <div class="breadcrumb">
            <a href="/">Dashboard</a> / Challenge 3
        </div>

        <div class="challenge-header">
            <h1>💉 Challenge 3 - SQL Injection</h1>
            <p class="subtitle">Contournez l'authentification via injection SQL</p>
        </div>

        <div class="card">
            <h2>📚 Description</h2>
            <p>
                Ce formulaire de connexion est vulnérable aux injections SQL. 
                Exploitez cette faille pour vous connecter sans connaître le mot de passe.
            </p>
        </div>

        <div class="card">
            <h2>🔑 Formulaire de connexion</h2>
            <form action="/challenge3/login" method="POST" class="login-form">
                <div class="form-group">
                    <label for="username">Username:</label>
                    <input type="text" id="username" name="username" required>
                </div>
                <div class="form-group">
                    <label for="password">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit" class="btn">Se connecter</button>
            </form>

            <% if (message) { %>
                <div class="message <%= message.type %>">
                    <%= message.text %>
                </div>
            <% } %>

            <% if (error) { %>
                <div class="error-display">
                    <strong>Erreur SQL exposée:</strong>
                    <pre><%= error %></pre>
                </div>
            <% } %>
        </div>

        <div class="card hints">
            <h2>💡 Indices</h2>
            <ul>
                <li>
                    Indice 1: 
                    <span class="spoiler">
                        <button class="btn-spoiler" onclick="this.nextElementSibling.classList.remove('hidden'); this.style.display='none'">👁️ Voir</button>
                        <span class="spoiler-content hidden">Testez avec des caractères spéciaux (' " ; --)</span>
                    </span>
                </li>
                <li>
                    Indice 2: 
                    <span class="spoiler">
                        <button class="btn-spoiler" onclick="this.nextElementSibling.classList.remove('hidden'); this.style.display='none'">👁️ Voir</button>
                        <span class="spoiler-content hidden">La requête SQL est probablement SELECT * FROM...</span>
                    </span>
                </li>
                <li>
                    Indice 3: 
                    <span class="spoiler">
                        <button class="btn-spoiler" onclick="this.nextElementSibling.classList.remove('hidden'); this.style.display='none'">👁️ Voir</button>
                        <span class="spoiler-content hidden">Observez les messages d'erreur SQL</span>
                    </span>
                </li>
            </ul>
        </div>

        <div class="back-button">
            <a href="/" class="btn">← Retour au Dashboard</a>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>
